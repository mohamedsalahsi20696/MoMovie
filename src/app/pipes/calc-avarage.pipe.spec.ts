import { CalcAvaragePipe } from './calc-avarage.pipe';

describe('CalcAvaragePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcAvaragePipe();
    expect(pipe).toBeTruthy();
  });
});
